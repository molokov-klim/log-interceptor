# Changelog

Все значительные изменения в проекте **LogInterceptor** будут документироваться в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

## [0.1.0] - 2025-11-27

Первый стабильный релиз LogInterceptor! 🎉

### Added

#### Core Features
- **LogInterceptor** - основной класс для мониторинга и перехвата лог-файлов
- Неблокирующее выполнение с использованием watchdog
- Кроссплатформенная поддержка (Linux, Windows)
- Context manager support (`with` statement)
- Thread-safe операции

#### Filtering System
- **BaseFilter** - абстрактный базовый класс для фильтров
- **RegexFilter** - фильтрация по регулярным выражениям
  - Режимы: `match`, `whitelist`, `blacklist`
  - Case-sensitive и case-insensitive поиск
- **PredicateFilter** - фильтрация с помощью функций-предикатов
- **CompositeFilter** - композиция фильтров с логикой AND/OR

#### Buffer Management
- In-memory буферизация с настраиваемым размером
- FIFO стратегия переполнения
- Thread-safe операции с буфером
- Методы `get_buffered_lines()` и `clear_buffer()`

#### Callback System
- Регистрация множественных callback функций
- Асинхронная обработка событий
- Передача метаданных (line, timestamp, event_id)
- Обработка ошибок в callbacks без остановки мониторинга

#### State Management
- **Pause/Resume** - управление захватом логов без остановки мониторинга
- **Statistics** - `get_stats()` для получения метрик:
  - `lines_captured` - количество захваченных строк
  - `events_processed` - количество обработанных событий
  - `start_time` - время запуска
  - `uptime_seconds` - время работы

#### Metadata & Timestamps
- Метаданные для каждой строки (line, timestamp, event_id)
- `get_lines_with_metadata()` - получение данных с метаданными
- Опциональное добавление ISO 8601 timestamps в target_file
- Уникальные event_id для аудита

#### Advanced Features
- **Debounce механизм** - предотвращение дублирующихся событий
- **InterceptorConfig** - система конфигурации с пресетами
  - Aggressive, Balanced, Conservative presets
  - Настраиваемые параметры (debounce_interval, buffer_size, encoding, etc.)
- Обработка ротации файлов
- Обработка ошибок (PermissionError, OSError)
- Retry механизм с настраиваемым количеством попыток

#### Testing Infrastructure
- **MockLogWriter** - mock объект для тестирования
  - Синхронный и асинхронный (thread) режимы
  - Burst mode для множественных записей
  - Симуляция ротации файлов
- **Pytest fixtures** - готовые фикстуры для интеграции
- **92 unit и integration тестов** с coverage 93.90%
- **6 performance тестов** с pytest-benchmark
- Slow тесты для обработки больших файлов (50k+ строк)

#### Documentation
- Полная API документация (docs/API.md)
- README с примерами использования
- 5 рабочих примеров в examples/
  - Базовое использование
  - Фильтрация логов
  - Callback система
  - Pytest интеграция
  - Продвинутые возможности
- Google-style docstrings для всех публичных API
- Technical specifications
- Development plan (TDD)

#### CI/CD
- GitHub Actions workflows:
  - Автоматическое тестирование (Linux, Windows)
  - Поддержка Python 3.9-3.12
  - Ruff linting
  - Pyright type checking
  - Coverage reporting (Codecov)
  - Автоматическая публикация на PyPI
- Pre-commit hooks configuration
- Makefile для удобной разработки

#### Performance
- **5,000+ строк/секунду** на больших файлах
- Метаданные: ~14 μs
- Операции с буфером: ~27 μs
- Линейная масштабируемость
- Минимальные накладные расходы на фильтрацию (<3%)

### Changed
- N/A (первый релиз)

### Deprecated
- N/A (первый релиз)

### Removed
- N/A (первый релиз)

### Fixed
- N/A (первый релиз)

### Security
- N/A (первый релиз)

---

[Unreleased]: https://github.com/hash/log-interceptor/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/hash/log-interceptor/releases/tag/v0.1.0

